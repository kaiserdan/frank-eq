# Stage M handoff

Stage M0 is complete, fetched, independently verified, and adopted. This is the
live continuation guide; it does not alter the immutable Stage-A v3-2 evidence.

## Terminal result

```text
job:       frank-eq-moment-compute-m0
Slurm:     1970800
state:     COMPLETED 0:0
elapsed:   01:37:52
decision:  OPERATION_CLOSED_EVENTS_NOT_READABLE
evidence:  evidence/real_stage_m_olivia_m0/
```

The exact event algebra and atomic-retention checks pass. The projected moment
executor beats the cross-fitted direct response, but it is worse than the
historical marginal/independence executor in aggregate and for both models.
Joint out-degree events and two-path intersections fail the frozen
balanced-accuracy requirement for both Qwen3-4B and Qwen3-8B.

Stage M0 has no held sender, claim-bearing test role, receiver access, or claim
authority. The negative machine decision leaves
`successor_protocol_draft_authorized=false`.

## Verification

The scheduler, repository verifier, in-job independent verifier, artifact
hashes, role separation, exact public algebra, 1,824 predictions, metrics, and
machine decision all verify. The fetched run and logs match Olivia under a
checksum-only comparison.

The workstation's newer NumPy initially changed only two projection-adjustment
summary fields by at most `4.44e-16`; no scientific prediction field, metric,
gate, or decision changed. A temporary Python 3.10 / NumPy 2.2.6 environment
matching the run reproduces every prediction, metric, and decision exactly.
This diagnostic is preserved in the compact evidence package.

## Scientific disposition

Stage M asked whether an operation-closed joint-event basis could explain the
gap between readable atomic facts and failed nonlinear composition. Under this
frozen source/task contract, it does not. The simpler marginal executor remains
better, and the required high-order coordinates are not uniformly readable.

Follow the registered stop rule: stop the current graph/source line. Do not
submit another M0 job, enlarge the event basis, tune its calibration or gates,
train a one-shot Stage M compiler, or open a held/receiver/claim-bearing role.

## Current next action

There is no authorized experiment or cluster executable. Any continuation must
begin with a new task-level scientific question and a separately frozen
registration using fresh roles. Drafting such a question is not authorization
to run it.

## Reading order

1. `evidence/real_stage_m_olivia_m0/AUDIT.md`
2. `docs/22_MAIN_RESULTS_AUDIT_AND_STAGE_M.md`
3. `docs/23_STAGE_M_OPERATION_CLOSED_BASIS.md`
4. `docs/24_STAGE_M_OLIVIA_RUNBOOK.md`
5. `evidence/real_stagea_v3_olivia/AUDIT.md`
